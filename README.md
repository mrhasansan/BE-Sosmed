# BE-Sosmed

### 1. Install or setup database

###  2. Setup tables

```
CREATE TABLE Users (
    userName  varchar(255),
    email     varchar(255),
    password  varchar(255)
);
```

### 3. Install dependencies
